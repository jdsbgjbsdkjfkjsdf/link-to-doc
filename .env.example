# Google Docs API — service account JSON (single-line string)
# Create in Google Cloud Console → IAM & Admin → Service Accounts → Keys.
GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"YOUR_PROJECT_ID",...}

# ID of the Google Doc to append links to (from the doc URL: .../d/DOC_ID/edit)
GOOGLE_DOC_ID=your_google_doc_id_here

# Optional: timezone for timestamps (default: America/New_York)
# TZ=America/New_York
